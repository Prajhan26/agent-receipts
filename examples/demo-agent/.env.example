# Required: 0G wallet private key (funded with 0G tokens for storage uploads)
ZEROG_PRIVATE_KEY=0x...

# Optional: reuse the same agent keypair across runs
# If not set, a fresh keypair is generated each run
AGENT_PRIVATE_KEY=
AGENT_PUBLIC_KEY=
